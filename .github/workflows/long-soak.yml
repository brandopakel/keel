name: Long soak

# Generated by scripts/generate-long-soak-workflow.py; edit that, not this.
#
# The 48-hour gate, met on free runners. A GitHub-hosted job ends at six hours,
# so the run is 9 segments of 5h20m, each handing the deployment to
# the next: both logs, the replica's checkpoint, and the harness's record of
# every value it was ever acknowledged, verified on the new machine before
# anything is written. Each arm is its own chain, so one arm's failure stops
# that arm and nothing else; a failed segment's successors are skipped.
#
# What this is: 48 hours of one dataset and one pair of logs under continuous
# writes, crash recovery and compaction, across 8 planned restarts and
# 9 machines, on one binary. What it is not: 48 hours of process uptime. A
# leak that only shows past five hours of one process is invisible here; the
# growth bounds in soak.py are the check for that shape, applied every cycle.
# For true uptime, dispatch scheduled-soak.yml onto a self-hosted runner on a
# machine that can be kept for two days; see docs/long-run-gate.md.
#
# Every segment runs the binary built here once, so all 27 jobs validate
# the same bytes even if a Go patch release lands mid-run.

on:
  workflow_dispatch:
    inputs:
      segment_seconds:
        description: Workload seconds per segment
        default: '19200'
      cycle_seconds:
        description: Seconds between crash/recovery cycles
        default: '30'
      runner:
        description: Runner label for the segments
        default: ubuntu-latest
  schedule:
    - cron: '0 4 * * 6'

permissions:
  contents: read

jobs:
  build:
    name: build once
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16
        with:
          go-version-file: go.mod
          cache: true
      - name: Build
        run: |
          mkdir -p dist/long-soak
          go build -trimpath -o dist/long-soak/keel ./cmd/keel
          dist/long-soak/keel -version | tee dist/long-soak/version.txt
          sha256sum dist/long-soak/keel | tee dist/long-soak/keel.sha256
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: long-soak-binary
          path: dist/long-soak
          retention-days: 30
  p1-s1:
    name: protocol 1, recovery 1/9
    needs: build
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 1
      segments: 9
      previous: ''
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p1-s2:
    name: protocol 1, recovery 2/9
    needs: p1-s1
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 2
      segments: 9
      previous: 'long-soak-p1-s1'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p1-s3:
    name: protocol 1, recovery 3/9
    needs: p1-s2
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 3
      segments: 9
      previous: 'long-soak-p1-s2'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p1-s4:
    name: protocol 1, recovery 4/9
    needs: p1-s3
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 4
      segments: 9
      previous: 'long-soak-p1-s3'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p1-s5:
    name: protocol 1, recovery 5/9
    needs: p1-s4
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 5
      segments: 9
      previous: 'long-soak-p1-s4'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p1-s6:
    name: protocol 1, recovery 6/9
    needs: p1-s5
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 6
      segments: 9
      previous: 'long-soak-p1-s5'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p1-s7:
    name: protocol 1, recovery 7/9
    needs: p1-s6
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 7
      segments: 9
      previous: 'long-soak-p1-s6'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p1-s8:
    name: protocol 1, recovery 8/9
    needs: p1-s7
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 8
      segments: 9
      previous: 'long-soak-p1-s7'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p1-s9:
    name: protocol 1, recovery 9/9
    needs: p1-s8
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p1
      args: --replication-protocol 1 --primary-crash-every 3
      segment: 9
      segments: 9
      previous: 'long-soak-p1-s8'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s1:
    name: protocol 2 concurrent, recovery 1/9
    needs: build
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 1
      segments: 9
      previous: ''
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s2:
    name: protocol 2 concurrent, recovery 2/9
    needs: p2c-s1
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 2
      segments: 9
      previous: 'long-soak-p2c-s1'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s3:
    name: protocol 2 concurrent, recovery 3/9
    needs: p2c-s2
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 3
      segments: 9
      previous: 'long-soak-p2c-s2'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s4:
    name: protocol 2 concurrent, recovery 4/9
    needs: p2c-s3
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 4
      segments: 9
      previous: 'long-soak-p2c-s3'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s5:
    name: protocol 2 concurrent, recovery 5/9
    needs: p2c-s4
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 5
      segments: 9
      previous: 'long-soak-p2c-s4'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s6:
    name: protocol 2 concurrent, recovery 6/9
    needs: p2c-s5
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 6
      segments: 9
      previous: 'long-soak-p2c-s5'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s7:
    name: protocol 2 concurrent, recovery 7/9
    needs: p2c-s6
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 7
      segments: 9
      previous: 'long-soak-p2c-s6'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s8:
    name: protocol 2 concurrent, recovery 8/9
    needs: p2c-s7
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 8
      segments: 9
      previous: 'long-soak-p2c-s7'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2c-s9:
    name: protocol 2 concurrent, recovery 9/9
    needs: p2c-s8
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2c
      args: --replication-protocol 2 --concurrent --primary-crash-every 3
      segment: 9
      segments: 9
      previous: 'long-soak-p2c-s8'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s1:
    name: protocol 2 concurrent, continuous primary 1/9
    needs: build
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 1
      segments: 9
      previous: ''
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s2:
    name: protocol 2 concurrent, continuous primary 2/9
    needs: p2cc-s1
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 2
      segments: 9
      previous: 'long-soak-p2cc-s1'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s3:
    name: protocol 2 concurrent, continuous primary 3/9
    needs: p2cc-s2
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 3
      segments: 9
      previous: 'long-soak-p2cc-s2'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s4:
    name: protocol 2 concurrent, continuous primary 4/9
    needs: p2cc-s3
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 4
      segments: 9
      previous: 'long-soak-p2cc-s3'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s5:
    name: protocol 2 concurrent, continuous primary 5/9
    needs: p2cc-s4
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 5
      segments: 9
      previous: 'long-soak-p2cc-s4'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s6:
    name: protocol 2 concurrent, continuous primary 6/9
    needs: p2cc-s5
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 6
      segments: 9
      previous: 'long-soak-p2cc-s5'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s7:
    name: protocol 2 concurrent, continuous primary 7/9
    needs: p2cc-s6
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 7
      segments: 9
      previous: 'long-soak-p2cc-s6'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s8:
    name: protocol 2 concurrent, continuous primary 8/9
    needs: p2cc-s7
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 8
      segments: 9
      previous: 'long-soak-p2cc-s7'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
  p2cc-s9:
    name: protocol 2 concurrent, continuous primary 9/9
    needs: p2cc-s8
    uses: ./.github/workflows/soak-segment.yml
    with:
      arm: p2cc
      args: --replication-protocol 2 --concurrent --primary-crash-every 0
      segment: 9
      segments: 9
      previous: 'long-soak-p2cc-s8'
      seconds: ${{ github.event.inputs.segment_seconds || '19200' }}
      cycle_seconds: ${{ github.event.inputs.cycle_seconds || '30' }}
      runner: ${{ github.event.inputs.runner || 'ubuntu-latest' }}
